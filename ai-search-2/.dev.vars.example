# Copy to .dev.vars (gitignored) for local `wrangler dev` testing.
# Set real values as secrets in the Cloudflare dashboard / via `wrangler secret put` for production.

GEMINI_API_KEY=
TURNSTILE_SECRET_KEY=
